fn main() {
    println!("Hello, world!");
}

// Routine for Sender-:
// Wake up, and send the Offer to Market
// Pub/Sub based queue i guess, put the data in queue, receiver can subscribe to it and poll that whenever it wants
